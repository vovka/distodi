<div class="form">
  <h3>Sign up as user</h3>
  <p>Please sing up as <strong>user</strong> if you would like to track service history for your items as a person. It means that you do service for your items by yourself or you ask a service company do it for you. You can keep history of the service, print, safe or send service history.
    <br><br><strong> Important: you should provide automatically generated link for access to your Service company and ask to confirm that they done service for your itms. They will have 24 hours for it.</strong> <br><br></p>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="field">
        <%= f.label :email %><br/>
        <%= f.email_field :email, autofocus: true,placeholder: "mail@gmail.com" %>
      </div>

      <div class="field">
        <%= f.label :first_name %><br/>
        <%= f.text_field :first_name ,placeholder: "Your name"%>
      </div>

      <div class="field">
        <%= f.label :last_name %><br/>
        <%= f.text_field :last_name,placeholder: "Your last name" %>
      </div>

      <div class="field">
        <%= f.label :phone %><br/>
        <%= f.text_field :phone ,placeholder: "+123 234 345 678"%>
      </div>

      <div class="field">
        <%= f.label :country %><br/>
        <%= f.text_field :country ,placeholder: "Country"%>
      </div>

      <div class="field">
        <%= f.label :city %><br/>
        <%= f.text_field :city ,placeholder: "Your city"%>
      </div>

      <div class="field">
        <%= f.label :street %><br/>
        <%= f.text_field :street ,placeholder: "Street"%>
      </div>

      <div class="field">
        <%= f.label :postal_code %><br/>
        <%= f.text_field :postal_code ,placeholder: "12345"%>
      </div>

      <div class="field">
        <%= f.label 'Additional field' %><br/>
        <%= f.text_field :notice %>
      </div>

      <div class="field">
        <%= f.label 'Picture' %><br>
        <%= f.hidden_field :picture_cache %>
        <%= f.file_field :picture %>
      </div>

      <div class="field">
        <%= f.label :password %>
        <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br/>
        <%= f.password_field :password, autocomplete: "off" %>
      </div>

      <div class="field">
        <%= f.label :password_confirmation %><br/>
        <%= f.password_field :password_confirmation, autocomplete: "off" %>
      </div>

      <div class="actions">
        <%= f.submit "Sign up" %>
      </div>
  <% end %>

  <%= render "users/shared/links" %>
</div>