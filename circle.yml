test:
  override:
    - if [ $CIRCLE_BRANCH != 'master' ]; then bundle exec pronto run -f bitbucket_pr -c origin/master; fi
    - bundle exec rake

deployment:
  production:
    branch: master
    commands:
      - cap production deploy
